
/**
 * Method to add values, pass two integers.
 */
function addVales(firstValue, secondValue) {
	var result = 0;
	result = firstValue + secondValue; // Addvalues logic.
	return result;
}

/**
 * Method to mul values, pass two integers.
 */
function mulValues(firstValue, secondValue) {
	var result = 0;
	result = firstValue * secondValue; // Mul Logic
	return result;	
}

/**
 * Method to division values, pass two integers.
 */
function divValues(firstValue, secondValue) {
	var result = 0;
	result = firstValue / secondValue; // Divistion logic
	return result;	
}

/**
 * Method to Sub values, pass two integers.
 */
function subValues(firstValue, secondValue) {
	var result = 0;
	result = firstValue - secondValue; // Substraction Logic
	return result;	
}